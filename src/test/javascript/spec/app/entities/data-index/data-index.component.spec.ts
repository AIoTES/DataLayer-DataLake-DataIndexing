/* tslint:disable max-line-length */
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Observable, of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, Data } from '@angular/router';

import { DataIntergrationTestModule } from '../../../test.module';
import { DataIndexComponent } from 'app/entities/data-index/data-index.component';
import { DataIndexService } from 'app/entities/data-index/data-index.service';
import { DataIndex } from 'app/shared/model/data-index.model';

describe('Component Tests', () => {
    describe('DataIndex Management Component', () => {
        let comp: DataIndexComponent;
        let fixture: ComponentFixture<DataIndexComponent>;
        let service: DataIndexService;

        beforeEach(() => {
            TestBed.configureTestingModule({
                imports: [DataIntergrationTestModule],
                declarations: [DataIndexComponent],
                providers: [
                    {
                        provide: ActivatedRoute,
                        useValue: {
                            data: {
                                subscribe: (fn: (value: Data) => void) =>
                                    fn({
                                        pagingParams: {
                                            predicate: 'id',
                                            reverse: false,
                                            page: 0
                                        }
                                    })
                            }
                        }
                    }
                ]
            })
                .overrideTemplate(DataIndexComponent, '')
                .compileComponents();

            fixture = TestBed.createComponent(DataIndexComponent);
            comp = fixture.componentInstance;
            service = fixture.debugElement.injector.get(DataIndexService);
        });

        it('Should call load all on init', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new DataIndex('123')],
                        headers
                    })
                )
            );

            // WHEN
            comp.ngOnInit();

            // THEN
            expect(service.query).toHaveBeenCalled();
            expect(comp.dataIndices[0]).toEqual(jasmine.objectContaining({ id: '123' }));
        });

        it('should load a page', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new DataIndex('123')],
                        headers
                    })
                )
            );

            // WHEN
            comp.loadPage(1);

            // THEN
            expect(service.query).toHaveBeenCalled();
            expect(comp.dataIndices[0]).toEqual(jasmine.objectContaining({ id: '123' }));
        });

        it('should re-initialize the page', () => {
            // GIVEN
            const headers = new HttpHeaders().append('link', 'link;link');
            spyOn(service, 'query').and.returnValue(
                of(
                    new HttpResponse({
                        body: [new DataIndex('123')],
                        headers
                    })
                )
            );

            // WHEN
            comp.loadPage(1);
            comp.reset();

            // THEN
            expect(comp.page).toEqual(0);
            expect(service.query).toHaveBeenCalledTimes(2);
            expect(comp.dataIndices[0]).toEqual(jasmine.objectContaining({ id: '123' }));
        });
        it('should calculate the sort attribute for an id', () => {
            // WHEN
            const result = comp.sort();

            // THEN
            expect(result).toEqual(['id,asc']);
        });

        it('should calculate the sort attribute for a non-id attribute', () => {
            // GIVEN
            comp.predicate = 'name';

            // WHEN
            const result = comp.sort();

            // THEN
            expect(result).toEqual(['name,asc', 'id']);
        });
    });
});
